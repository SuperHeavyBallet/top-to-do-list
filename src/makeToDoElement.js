export function ToDoElement(name, description, dueDate, priority, notes, checklist) {
    return {
        name: name,
        description: description,
        dueDate: dueDate,
        priority: priority,
        notes: notes,
        checklist: checklist
    };
  }